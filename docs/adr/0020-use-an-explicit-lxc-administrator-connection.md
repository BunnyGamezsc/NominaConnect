# Superseded: control LXCs through the Proxmox host

Superseded by ADR-0032. NominaConnect's Proxmox-shell workflow uses `pct exec`
from the Proxmox host rather than a separate administrator connection to each
LXC.
