# Support Caddy and Traefik as reverse-proxy choices

NominaConnect's initial platform bootstrap will offer Caddy and Traefik as
complete reverse-proxy provider choices. Each choice must provide setup,
inspection, and adoption support; the CLI must not advertise an option that is
only partially implemented. This keeps the meaningful proxy architecture
decision explicit for users.
