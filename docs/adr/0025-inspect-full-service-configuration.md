# Inspect full service configuration

Service plugins must inspect and adopt their managed application's configuration
as well as DNS, reverse-proxy, certificate, VPN, and deployment state.
NominaConnect is therefore not limited to infrastructure integration fields; it
keeps its managed inventory connected to the observable configuration of every
supported service.
