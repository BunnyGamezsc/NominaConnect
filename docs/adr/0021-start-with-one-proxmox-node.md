# Start with one explicit Proxmox node

The first `nomina init` targets one explicit Proxmox node. The node remains
explicit in configuration so future cluster support can extend the model without
changing service identity or moving a hidden default.
