# Provide the full initial platform catalog

The initial platform choices are Technitium, Caddy, Traefik, step-ca, Caddy
Internal CA, Tailscale, and NetBird. Each selectable option must implement the
complete setup, inspection, and adoption contract; users choose named software
instead of a hidden generic capability.
